{
  "method": "Single-shot structural UE5.3 prompt evaluation. Code lines come from fenced blocks; correctness uses deterministic engine-API/safety gates. No Unreal runtime performance is claimed.",
  "configs": [
    "baseline — no skill",
    "caveman — terse-prose control",
    "ponytail — Ponytail UE5.3 full instructions"
  ],
  "tasks": [
    { "id": "timer", "prompt": "In Unreal Engine 5.3 C++, make an Actor respawn once after 5 seconds. It currently has Tick disabled." },
    { "id": "replication", "prompt": "Implement server-authoritative replicated health in Unreal Engine 5.3 C++ so remote clients and late joiners receive state." },
    { "id": "soft-asset", "prompt": "In UE5.3 C++, reference an optional skin that should remain unloaded until selected, then load it safely." },
    { "id": "private-dependency", "prompt": "A private cpp file needs TargetModule in this UE5.3 module. Show the smallest correct Build.cs dependency change." },
    { "id": "editor-assets", "prompt": "Batch rename Unreal Engine 5.3 assets while preserving references. Give the supported minimal editor automation path." }
  ]
}
